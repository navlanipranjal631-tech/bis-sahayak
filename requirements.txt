# BIS Standards RAG — BIS Sahayak
# Install with: pip install -r requirements.txt

# PDF parsing (Windows compatible — no external tools needed)
pdfplumber>=0.10.0

# Embeddings — neural (production)
sentence-transformers>=2.7.0
torch>=2.0.0

# Embeddings — TF-IDF (offline/dev fallback)
scikit-learn==1.3.2

# Vector search
faiss-cpu>=1.7.4

# LLM rationale generation
groq>=0.9.0

# UI
streamlit==1.32.0

# Core
numpy>=1.24.0
